import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.ffb_chit',
  appName: 'FFB_Chit',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;