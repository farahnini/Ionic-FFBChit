import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '2_2022_buluh',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
